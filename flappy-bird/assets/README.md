# Bird sprite

Drop your bird image here as `bird.png` (transparent background, roughly
square, ~64x64px works well) and the game will automatically use it in
place of the yellow placeholder box — no code changes required.
